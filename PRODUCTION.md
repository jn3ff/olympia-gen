2026-01-19 - added debug functionality to the default feature set for development purposes. the final game should be built without this tooling. For now, that command looks like:

```cargo build --release --no-default-features```
